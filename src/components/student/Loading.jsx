import React from 'react';

const Loading = () => {
    return (
        <div>
            <h1>Loading...</h1>
            <p>Please wait while we fetch the data.</p>
        </div>
    );
}

export default Loading;
